def get_bot_response(user_question: str) -> str:
    """
    Dummy backend function — Person C will replace this later.
    """
    return (
        "This is a dummy response from the backend. "
        "Person C will replace this with the real model on Day 2.\n\n"
        f"You asked: {user_question}"
    )
